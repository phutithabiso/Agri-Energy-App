<Page x:Class="Agri_Energy_App.ProductsPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="My Products">

    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="My Products" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <ItemsControl x:Name="productsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="0,0,0,15" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Category}" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding ProductionDate, StringFormat='Production Date: {0:yyyy/MM/dd}'}" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding Description}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="Edit" Width="80" Margin="5,0"
                                            Background="#228B22" Foreground="White"
                                            Click="EditProducts_Click"
                                            Tag="{Binding Id}"/>
                                    <Button Content="Delete" Width="80" Margin="5,0"
                                            Background="#B22222" Foreground="White"
                                            Click="DeleteProducts_Click"
                                            Tag="{Binding Id}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>