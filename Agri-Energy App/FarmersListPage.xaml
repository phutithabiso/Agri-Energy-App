<Page x:Class="Agri_Energy_App.FarmersListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="FarmersListPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="All Farmers" FontSize="20" FontWeight="Bold" Margin="10"/>

        <DataGrid x:Name="farmersGrid" Grid.Row="1" Margin="10" AutoGenerateColumns="False"
                 CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="120"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Date Registered" 
                                  Binding="{Binding DateRegistered, StringFormat=yyyy-MM-dd}" Width="120"/>

                <DataGridTextColumn Header="Last Login" 
                                  Binding="{Binding LastLogin, StringFormat=yyyy-MM-dd HH:mm}" Width="160"/>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>