<Page x:Class="Agri_Energy_App.ProductSearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Product Search">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="txtSearchQuery"
                     Width="400"
                     Height="30"
                     Margin="0,0,10,0"
                     Text="Enter product name..."
                     Foreground="Gray"
                     GotFocus="TxtSearchQuery_GotFocus"
                     LostFocus="TxtSearchQuery_LostFocus"/>

            <Button Content="Search"
                    Width="100"
                    Height="30"
                    Background="#228B22"
                    Foreground="White"
                    Click="BtnSearch_Click"/>
        </StackPanel>

        <!-- Results List -->
        <ListBox x:Name="lstSearchResults" Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="5">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{Binding Category}" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding ProductionDate, StringFormat='Production Date: {0:yyyy/MM/dd}'}" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>